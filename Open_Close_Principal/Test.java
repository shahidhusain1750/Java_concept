public class Test {
    public static void main(String[] args) {
        Bike b = new Bike();
        // b.running(10);
        // b.Stop(5);

        Car car = new Bike();
        car.running(100);

        // we cannot modify the properties in interface becaus its final

        // car.s = 746;
    }
}
